import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 5,
  },

  textIput: {
    marginBottom: 10,
    borderWidth: 1,
    color: '#000',
    fontSize: 16,
  },

  text: {
    fontSize: 22,
    alignSelf: 'center',
  },

  button: {
    padding: 5,
    borderWidth: 1,
    borderRadius: 5,
    alignItems: 'center',
    marginTop: 10,
  },

  buttonText: {
    fontSize: 16,
  },
});

export default styles;
